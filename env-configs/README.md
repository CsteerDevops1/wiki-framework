This are the examples of .env files, they should be configured and passed to the corresponding folders 
(e.g. ```cp userBot.env telegramBots/userBot/.env```)

dumb and restore scripts are supposed to be used with mongodb running inside of container
